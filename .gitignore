# Raw data — redistributed via the source release, not committed (see Data Availability)
data/raw/*.csv
data/raw/*.csv.gz
data/raw/*.json
data/raw/*.jsonl
# Working databases / large intermediates
*.duckdb
*.duckdb.wal
data/processed/*.duckdb
# R / Python cruft
.Rhistory
.RData
.Rproj.user/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
# Manuscript and submission files kept private (the paper is public on medRxiv)
manuscript/
submission/
# Keep: scripts (R/, python/), results/, figures/, docs/, and small processed CSVs
!data/processed/*.csv
